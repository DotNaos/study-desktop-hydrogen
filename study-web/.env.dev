#! These are only placeholders and should be overriden.
#! I am aware you don't commit secrets, but this file should ONLY be used in an enviroment that does NOT version the code.
###? Run: docker compose -f docker-compose.yaml --env-file .env.dev --profile dev -p aryazos-study-web-dev up -d
ENV=dev
HOST_PORT=5173
CONTAINER_PORT=5173

VITE_OIDC_AUTHORITY=https://aryazos.localhost/application/o/aryazos/
VITE_OIDC_CLIENT_ID=aryazos
